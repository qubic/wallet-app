import 'package:flutter/material.dart';

class StoredTransactionsForId extends StatelessWidget {
  const StoredTransactionsForId({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
